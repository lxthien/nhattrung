{% extends 'layout.html.twig' %}

{% block title %}{{ post.getPageTitle() is not null ? post.getPageTitle() : post.getTitle() }}{% endblock %}
{% block description %}{{ post.getPageDescription() is not null ? post.getPageDescription() : post.getDescription() }}{% endblock %}
{% block keyword %}{{ post.getPageKeyword() }}{% endblock %}

{% block structuredOpenGraph %}
    <meta property="og:type" content="article" />
    <meta property="og:title" content="{{ post.getPageTitle() is not null ? post.getPageTitle() : post.getTitle() }}" />
    <meta property="og:description" content="{{ post.getPageDescription() is not null ? post.getPageDescription() : post.getDescription() }}" />
{% endblock %}

{% block body %}
    <div class="wrapper-container-item-detail">
        <div class="container wrapper-container contacts page-detail">
            <div class="row">
                {{ include('news/components/breadcrumbs.html.twig') }}

                <div class="col-xs-12 col-sm-12 col-md-8">
                    <div class="company">
                        <h3 class="text-uppercase">TƯ VẤN THIẾT KẾ XÂY DỰNG NHẬT TRUNG</h3>
                        <ul>
                            <li>
                                <i class="fa fa-map-marker"></i>
                                <span><strong>VPGD: 150 Phạm Văn Bạch, Phường 15, Tân Bình, TP HCM</strong></span>
                            </li>
                            <li>
                                <i class="fa fa-phone"></i>
                                <span><strong>Hotline: 0973 40 01 38 - 0936 40 41 39 A Tuần</strong></span>
                            </li>
                            <li>
                                <i class="fa fa-envelope-o"></i>
                                <span><strong>Email: xaydungnhatthu@gmail.com</strong></span>
                            </li>
                            <li>
                                <i class="fas fa-money-check"></i>
                                <span><strong>MST: 0314251798</strong></span>
                            </li>
                            <li>
                                <i class="fa fa-internet-explorer"></i>
                                <span><strong>Website: www.xaydungnhattrung.net</strong></span>
                            </li>
                        </ul>
                    </div>

                    <div class="maps">
                        <iframe src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3918.8545761162104!2d106.63726426018289!3d10.822438955477033!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x31752965b8f0c037%3A0x84c6efa6e50825b3!2zMTUwIFBo4bqhbSBWxINuIELhuqFjaCwgUGjGsOG7nW5nIDE1LCBUw6JuIELDrG5oLCBI4buTIENow60gTWluaCwgVmlldG5hbQ!5e0!3m2!1sen!2s!4v1564194080270!5m2!1sen!2s" width="100%" height="400" frameborder="0" style="border:0" allowfullscreen></iframe>
                    </div>
                
                    {% for message in app.flashes('error') %}
                        <div class="alert alert-danger" role="alert">
                            {{ message }}
                        </div>
                    {% endfor %}
                    {% for message in app.flashes('notice') %}
                        <div class="alert alert-success" role="alert">
                            {{ message }}
                        </div>
                    {% endfor %}

                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    {{ form_end(form) }}
                </div>

                {{ include('layout/sidebar.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}